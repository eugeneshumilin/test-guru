<h2>Test <%= @test.title %> have <%= 'question'.pluralize(@test.questions.count) %> </h2>
<br>
<table>
  <tr>
    <th>Question</th>
    <th colspan="3">Action</th>
  </tr>
  <%= render @test.questions %>
</table><br>
<h3><%= link_to 'Create new question', new_test_question_path(@test) %></h3><br>
<%= link_to 'Back', tests_path %>